import React from 'react';

function AppointmentForm ({customer}) {
    return (
        <form id="appointment">
            <select name="service">
                <option>Test</option>
            </select>
        </form>
    );
}

export default AppointmentForm;